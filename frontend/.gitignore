# Frontend (Vite + React)
node_modules/
# dist/ is intentionally TRACKED so PCs that cannot run `npm build`
# (no Node / no npm-registry access) can deploy via `git pull`.
dist-ssr/
*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.vite/
.DS_Store
