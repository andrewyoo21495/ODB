"""Checklist visualization helpers.

Each module provides render functions that produce PNG images for a specific
checklist rule.  The images are returned as file paths and embedded into the
Excel report by the reporter.
"""
